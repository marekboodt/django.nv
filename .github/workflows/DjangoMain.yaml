name: Django SAST Pipeline

on:
  push:
    branches:
      - main

jobs:
  trigger-sast-scan:
#    uses: marekboodt/Security-Scanning-Repo/.github/workflows/sast-workflow.yml@main
    uses: marekboodt/Security-Scanning-Repo/blob/main/.github/workflows/sast-workflow.yml@main
    with:
      language: python
      project_dir: ./src
      environment: non-prod
    secrets:
      token: ${{ secrets.SECURITY_REPO_TOKEN }}
